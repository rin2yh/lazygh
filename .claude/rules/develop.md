---
paths:
  - "**/*"
---


# 概要

開発プロセスに関するルールを記載する。

## 開発完了前

以下のコマンドを実行すること

- `go fmt ./...`：フォーマッター
- `go vet ./...`：リンター
- `go test ./...`：テスト

## git commit 前

git commit を実行する前に `/simplify` を2回実行してコードをシンプルにすること。

