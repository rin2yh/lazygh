---
paths:
  - "**/*"
---


# 概要

開発プロセスに関するルールを記載する。

## 開発完了前

以下のコマンドを実行すること

- `go fmt ./...`：フォーマッター
- `go vet ./...`：リンター
- `go test ./...`：テスト

実装やテストの完了後に `/simplify` スキルを使用し、**必ず**リファクタリングすること

